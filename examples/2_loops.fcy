func main() {
    // endless loop (not really)
    while true {
        printintrin 0;
        break;
    }

    // silly stuff 
    while true break;

    // lets print every integer from 0 to 10 exclusively
    for let a: int = 0 in a < 10; a = a + 1 {
        printintrin a;
    }

    // shorter way
    for let a: int = 0 in a < 10; a = a + 1 printintrin a; 

    return;
}
