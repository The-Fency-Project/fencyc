func main() {
    // test continues
    for let a: int = 0 in a < 10; a = a + 1 { // += operator currently won't 
                                    // work inside for loop declaration
        if a % 3 == 0 {
            continue;
        } 
        printintrin a; 
    }
}
