func main() {
    let arr: [int] = [1, 2, 3]int;
    let l: uint = 3u;

    for let i: uint = 0u in i < l; i += 1u {
        printintrin arr[i];
    }

    let s: str = "Hello, world!";
    print_s(s);

    let arr_s: [str] = ["hello", "world"]str;
    printintrin arr_s[1u];

    return;
}

func print_s(s1: str) {
    printintrin s1;
    return;
}
