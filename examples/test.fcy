// Compile with 
// `fencyc input examples/test.fcy -o testbins/test --ldflags lm`

extern func sqrt(a: double) -> double;

func main() {
    let c: double = sqrt(16.0); 
    printintrin c;

    while (true) {
        return;
    }

    return;
}

