func main() {
    let warning_test: float = (1.5)$float;
    printintrin warning_test;

    let to_uint: uint = conv(-2.5);
    printintrin to_uint; // expected: 2 

    let to_int: int = conv(-2.5);
    printintrin to_int; // expected: -2 

    return;
}

func conv(a: float) -> uint {
    if a < 0.0 {
        return (-a)$uint;
    }
    return a$uint;
}

override func conv(a: float) -> int {
    return a$int;
}
